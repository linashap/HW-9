'''Создать lambda функцию, которая принимает на вход неопределенное
количество именных аргументов и выводит словарь с ключами удвоенной
длины. {‘abc’: 5} -> {‘abcabc’: 5}
'''

def dictt()->dict:#функция с помощью которого можно бесконечно вводить значения в словарь над которым будем работать
    di={}
    while True:
        key=(input("Введите ключ,оставьте его пустым для прекращения создания элементов словаря "))
        if key=="":
            break
        if key!="":
           value=input("Введите значение ")
        di[key]=value
    return di
from functools import reduce
rez=dictt ()

def main():
   dic={}#пустой словарь вывода пыталась сделать с помощью конструктора 
   #но не понимала как нормально достать и ключ и значения из кортежей одновременно
   values=tuple(value for value in rez.values())#значения словаря
   keys=tuple(map(lambda key:key+key,rez.keys()))#удвоение ключей лямбда функцией
   for key in keys:
       for value in values:
           dic[key]=value#заносим в ловарь удвоенный ключь и его значение которое было в исх словаре
   print(dic)

if __name__=="__main__":
    main()
  